<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="Card" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource CardBrush}"/><Setter Property="CornerRadius" Value="12"/>
        <Setter Property="Padding" Value="22"/><Setter Property="BorderBrush" Value="{StaticResource LineBrush}"/><Setter Property="BorderThickness" Value="1"/>
    </Style>
    <Style x:Key="MetricCard" TargetType="Border" BasedOn="{StaticResource Card}">
        <Style.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="{StaticResource HoverBrush}"/></Trigger></Style.Triggers>
    </Style>
    <Style x:Key="StatusBadge" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/><Setter Property="CornerRadius" Value="6"/>
        <Setter Property="Padding" Value="10,5"/><Setter Property="HorizontalAlignment" Value="Left"/>
    </Style>
    <Style x:Key="ConnectionStatus" TargetType="TextBlock" BasedOn="{StaticResource Caption}">
        <Style.Triggers>
            <Trigger Property="Text" Value="Connected"><Setter Property="Foreground" Value="{StaticResource SuccessBrush}"/></Trigger>
            <Trigger Property="Text" Value="Disconnected"><Setter Property="Foreground" Value="{StaticResource CriticalBrush}"/></Trigger>
            <Trigger Property="Text" Value="Active"><Setter Property="Foreground" Value="{StaticResource TealBrush}"/></Trigger>
            <Trigger Property="Text" Value="Idle"><Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/></Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
